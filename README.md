# Home Server Project Renovate

Central self-hosted Renovate runner for Home Server Project repositories.

Repositories are enrolled explicitly, one at a time. The initial target is `home-server-project/home-server-packages`.
